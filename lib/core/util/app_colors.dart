import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors (Soft Red Theme)
  static const Color primary = Color(0xFFE53935); // Soft Red
  static const Color lightBackground = Color(0xFFFFF8F7); // Very light coral
  static const Color lightCard = Color(0xFFFFEBEE); // Light red accent
  static const Color lightText = Color(0xFF212121); // Dark Gray for text
  static const Color lightIcon =
      Color(0xFF5D4037); // Brownish tint for contrast
  static const Color lightNavBar = Color(0xFFFFCDD2); // Light red navbar

  // Dark Theme Colors (Red + Light Black)
  static const Color primaryDark = Color(0xFFD32F2F); // Red
  static const Color darkBackground = Color(0xFF1A1A1A); // Light Black
  static const Color darkCard = Color(0xFF2A2A2A);
  static const Color darkText = Color(0xFFFFFFFF);
  static const Color darkIcon = Color(0xFFCCCCCC);
  static const Color darkNavBar = Color(0xFF2B2B2B);

  // Divider Colors
  static const Color divider = Color(0xFFBDBDBD);
  static const Color dividerDark = Color(0xFF757575);
}
